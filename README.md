# rateguard-distributed-api-gateway
FastAPI API gateway with a Redis-backed distributed sliding-window rate limiter, JWT authentication, admin RBAC, hashed API keys, JWT revocation, reverse proxying to downstream microservices, and a React + TypeScript dashboard.
